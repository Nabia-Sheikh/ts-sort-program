//This code defines an interface `Sortable` that specifies a `sort()` method. Any class that implements this interface must provide an implementation of the `sort()` method.
export interface Sortable {
  sort(): void
}
